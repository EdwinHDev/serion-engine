{
  "name": "serion-engine",
  "private": true,
  "description": "Professional Web-based 3D Game Engine",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:editor": "npm run dev --workspace=@serion/editor",
    "build:engine": "npm run build --workspace=@serion/engine",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
